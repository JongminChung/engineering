# AGENTS.md

## 범위

- Spectral 규칙/스키마 실험 프로젝트에 대한 규칙입니다.

## 작업 원칙

- `node_modules`는 수정하지 않습니다.
- 규칙 변경 시 `ruleset.ts`와 관련 문서(`readme.md`)를 함께 점검합니다.
- 샘플 스펙은 `petstore.yaml`을 기준으로 유지합니다.

## 포맷/의존성

- TypeScript 포맷은 루트 `biome.json`과 `.editorconfig`를 따릅니다.
- 의존성 변경이 필요하면 `package.json`을 기준으로 합니다.
